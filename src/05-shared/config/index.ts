import { TGlobalSettings } from '@app-types'

export const globalStyleSettings: TGlobalSettings = {
	color: '#1441c2',
	textColor: '#ffffff',
	fontSize: 16,
	lineColor: '#fed500',
}

/* Жирность шрифта для жирного выделения */
export const boldVariable = 700
